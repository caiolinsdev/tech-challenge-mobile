# ===========================================
# TECH CHALLENGE - VARIÁVEIS DE AMBIENTE
# ===========================================

# Database (PostgreSQL)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=blog_db
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/blog_db

# API
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=sua-chave
JWT_EXPIRES_IN=7d

# Mobile (Expo) - URL da API para o app
# Use localhost para emulador; use o IP do seu PC para celular físico
# Ex: API_URL=http://(Seu IP):3000
API_URL=http://localhost:3000
